name: CI

on:
  push:
    branches:
      - main
      - 'release/**'
      - 'feature/**'
  pull_request:
    branches:
      - '**'
  workflow_dispatch:
    inputs:
      publish_docker:
        description: 'Publish Docker image to Docker Hub'
        required: true
        type: boolean
        default: false
      docker_tag:
        description: 'Docker image tag (e.g., latest, v1.0.0)'
        required: false
        type: string
        default: 'v0.0.1'

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build pkg-config

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Bootstrap submodules
        run: ./scripts/bootstrap.sh

      - name: Sync LeanSpec environment
        working-directory: tools/leanSpec
        run: |
          uv python install 3.12
          uv sync

      - name: Generate networking fixtures
        run: uv run --directory tools/leanSpec python ../../tools/fixtures/generate_networking_ssz.py

      - name: Configure (CMake)
        run: cmake -S . -B build -GNinja -DLANTERN_BUILD_TESTS=ON

      - name: Build
        run: cmake --build build --parallel

      - name: Test
        run: ctest --test-dir build --output-on-failure -V

  publish-docker-amd64:
    name: Publish Docker Image (amd64)
    runs-on: ubuntu-24.04
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.publish_docker == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push (amd64)
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/lantern:${{ github.event.inputs.docker_tag }}-amd64
          build-args: |
            GIT_COMMIT=${{ github.sha }}
            GIT_BRANCH=${{ github.ref_name }}
          cache-from: type=gha,scope=lantern-docker-amd64
          cache-to: type=gha,mode=max,scope=lantern-docker-amd64

  publish-docker-arm64:
    name: Publish Docker Image (arm64)
    runs-on: ubuntu-24.04-arm
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.publish_docker == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push (arm64)
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/arm64
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/lantern:${{ github.event.inputs.docker_tag }}-arm64
          build-args: |
            GIT_COMMIT=${{ github.sha }}
            GIT_BRANCH=${{ github.ref_name }}
          cache-from: type=gha,scope=lantern-docker-arm64
          cache-to: type=gha,mode=max,scope=lantern-docker-arm64

  publish-docker-manifest:
    name: Publish Docker Manifest
    runs-on: ubuntu-24.04
    needs:
      - publish-docker-amd64
      - publish-docker-arm64
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.publish_docker == 'true'
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create and push manifest list
        run: |
          docker buildx imagetools create \
            -t ${{ secrets.DOCKERHUB_USERNAME }}/lantern:${{ github.event.inputs.docker_tag }} \
            ${{ secrets.DOCKERHUB_USERNAME }}/lantern:${{ github.event.inputs.docker_tag }}-amd64 \
            ${{ secrets.DOCKERHUB_USERNAME }}/lantern:${{ github.event.inputs.docker_tag }}-arm64

      - name: Inspect manifest list
        run: |
          docker buildx imagetools inspect \
            ${{ secrets.DOCKERHUB_USERNAME }}/lantern:${{ github.event.inputs.docker_tag }}
